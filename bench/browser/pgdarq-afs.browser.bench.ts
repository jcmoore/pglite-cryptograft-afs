import { bench, describe } from 'vitest'

describe('pgdarq-afs browser benchmarks', () => {
  bench.skip('browser-worker benchmark scaffold', () => {
    // The real browser-worker benchmarks will live here once the harness
    // is wired into a cross-origin isolated preview server.
  })
})
